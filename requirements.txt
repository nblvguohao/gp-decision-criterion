# The exact environment in which every result in the paper was generated
# (macOS 26.6.2, Apple silicon; final run 2026-09-24).
# The analysis uses only the standard scientific stack; no compiled extensions,
# no R, and no GPU. R serialization (.RData) is read by a pure-Python reader
# included in the archive (analysis/crosscrop/code/rdata.py), so R is not needed.
numpy==2.4.6
scipy==1.17.1
pandas==2.3.3
matplotlib==3.11.1
pillow==12.3.0          # installed with matplotlib; fig6.py reads the Fig. 6A screenshots
scikit-learn==1.9.1
statsmodels==0.14.6
openpyxl==3.1.5        # only for the withdrawn CAIGE wheat panel (.xlsx workbooks)
